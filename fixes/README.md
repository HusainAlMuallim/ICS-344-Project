# Fix Workspace

`fixes/lesson-XX/` is used to store:
- policy snippets
- code diffs
- before/after notes
- deployment steps
- verification notes

The repo brief values real fixes, not cosmetic front-end-only changes. Each fix should map to the actual vulnerable backend, IAM, or workflow layer.
